# Hello-world GitHub Pages

Dobrodošli v Hello-world repozitoriju!  
Ta repozitorij je nastavljen za osnovno GitHub Pages stran.

## Struktura repozitorija

- `index.html` – glavna HTML stran
- `assets/` – CSS, slike in ostale statične datoteke
- `.github/workflows/ci-cd.yml` – CI/CD workflow (smoke test + deploy)
- `smoke-check.py` – preprosti smoke test za preverjanje strani

## Urejanje strani

1. Ustvari novo vejo:
   ```bash
   git checkout -b feature/moja-sprememba
